<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= day.name %>
    </title>
</head>

<body>
    <h1>Show Page</h1>
    <p>
        <%= day.name %>
    </p>
    <div>
        Events:
        <div>
            <% for(let event of day.events) { %>
                <a style="display:inline-block; text-decoration: none; color: inherit;"
                    href="/sportsDays/<%= day._id %>/events/<%= event._id %>">
                    <div style="border: 2px solid black; border-radius: 6px; display: inline-block;">
                        <p>
                            <%= event.name %>
                        </p>
                        Participants:
                        <ul>
                            <% for(let participant of event.participants) { %>
                                <li>
                                    <%= participant %>
                                </li>
                                <% } %>
                        </ul>
                    </div>
                </a>
                <% } %>
        </div>
    </div>

    <form action="/sportsDays/<%= day._id %>/events/new">
        <button>Add event</button>
    </form>

    <form action="/sportsDays/<%= day._id %>/edit">
        <button>Edit</button>
    </form>

    <form action="/sportsDays/<%= day._id %>?_method=DELETE" method="POST">
        <button>Delete</button>
    </form>

    <form action="/sportsDays">
        <button>All Sports Days</button>
    </form>
</body>

</html>